- multiple LUNs
- SCSI TRIM support
- multiple concurrent sessions
  - SCSI RESERVE
- VMware initiator compatibility
- nmap 'compatibility'
- configurable:
  - async writes
  - name of disk-image on SD card
- serial number so to allow linux to see which is which
- ntp upstream for logging timestamps?
